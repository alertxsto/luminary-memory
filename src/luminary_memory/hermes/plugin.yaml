name: luminary
version: 0.2.9
description: "Luminary Memory — self-hosted memory with 4-strategy recall (semantic, keyword, temporal, graph), RRF fusion, and lifecycle maintenance."
pip_dependencies:
  - "luminary-memory>=0.2.6"
requires_env: []
hooks:
  - on_session_end
